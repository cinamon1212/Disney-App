import { LoaderSizeProps } from 'react-spinners/helpers/props'

export const styles = {
  color: 'white',
  cssOverride: {
    position: 'absolute',
    top: '50%',
    left: '50%',
    transform: 'translate(-50%, -50%)',
  },
  size: 200,
} as LoaderSizeProps
